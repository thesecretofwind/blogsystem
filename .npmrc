# 是否提升依赖,如果某些工具仅在根目录的node_modules时才有效,可以将其设置为true来提升那些不再根目录的node_modules
# 就是将你安装的依赖包的依赖包的依赖包...都放到同一级别(扁平化). 
shamefully-hoist=true
# 当peerDependencies错误时,命令是否成功
strict-peer-dependencies=false
ignore-workspace-root-check=true